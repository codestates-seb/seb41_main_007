spring:
  profiles:
    active: bbamsoo
    include: secret

logging:
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG
server:
  servlet:
    encoding:
      force-response: true